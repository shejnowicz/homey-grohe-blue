# GROHE Blue Homey Pro verification record

Record started: 2026-08-20 (Europe/Warsaw)
Scope of this record: local/documentation portion only

## Status vocabulary

- **PASS** — command or behavior was executed locally and evidence is recorded below.
- **PENDING** — requires the authorized Homey Pro, GROHE account, or real appliance and was not executed.
- **UNAVAILABLE** — required tooling is not present; this is not a pass.

No Homey hub was selected, no developer app was installed, no account was paired, and no real appliance state was read or changed during this local phase.

## Local evidence

| Check | Status | Evidence |
| --- | --- | --- |
| Automated unit/integration tests | PASS | `npm test`: 70 tests, 70 pass, 0 fail |
| Project lint script | PASS | `npm run lint`: exit 0 |
| Homey manifest/publish validation | PASS | `homey app validate`: validated successfully at `publish` level |
| Patch whitespace | PASS | `git diff --check`: exit 0, no output |
| Aikido generated-code scan | UNAVAILABLE | Aikido MCP is unavailable in the current tool set; no scan result exists |

Exact command transcript:

```text
$ npm test
tests 70
pass 70
fail 0
exit_code=0

$ npm run lint
> node --check lib/redact.js test/redact.test.js
exit_code=0

$ homey app validate
✓ Pre-processing app...
✓ Validating app...
✓ App validated successfully against level `publish`
exit_code=0

$ git diff --check
exit_code=0
```

The shell emitted an unrelated macOS Java Runtime discovery notice before commands. Exit codes were checked independently.

## MVP verification matrix

| Criterion | Status | Required evidence / result |
| --- | --- | --- |
| SDK v3/Homey Compose manifest is publish-valid | PASS | Local Homey publish validation above |
| OIDC, refresh, dashboard, PUT, mapping, polling, availability, rollback, lifecycle, and Flow behavior | PASS | Local automated suite above |
| Intended Homey Pro is selected and owner-confirmed | PENDING | Run `homey select current`, show only non-sensitive hub identity to owner, obtain confirmation before install |
| Developer app installs/runs without secret-bearing logs | PENDING | Authorized CLI run/install transcript, sanitized before storage |
| GROHE account pairs through Homey UI; password is not persisted | PENDING | Pairing result plus private-storage review without exposing values |
| Every type-104 Blue Home appliance is discovered with correct identity | PENDING | Compare Homey device list to Watersystems; redact immutable/routing IDs |
| Online, filter/CO₂ percentages and liters, timestamp, idle time, and counters match | PENDING | Side-by-side Homey/Watersystems or sanitized API observations |
| Low-level alarms and Flow threshold triggers cross boundaries once | PENDING | Controlled observation without disrupting household automations |
| Device-tile auto flush enables and API/Homey confirm it | PENDING | Record initial state, one command, confirmation, and sanitized timestamps |
| Enable and disable Flow actions both confirm state | PENDING | Run each action once; no repeated PUT after ambiguity |
| Original automatic-flushing state is restored | PENDING | Final Homey/API state equals recorded initial state |
| One/two/three read failures and later recovery follow availability policy | PENDING | Prefer mocked/controlled network failure; do not disrupt other automations |
| Re-login restores an expired session without terminal credentials | PENDING | Use Homey pairing UI only; no shell/history/repository credentials |
| No secrets exist in committed artifacts or retained logs | PENDING | Repeat repository/log review after live work and before committing evidence |

## Authorized live procedure (not yet executed)

1. Ask the owner to identify the intended Homey Pro. Run `homey select current` only after authorization and confirm the returned non-sensitive identity with the owner.
2. Start/install the developer app. Capture only the minimum necessary log lines and redact authorization/cookie headers, email, tokens, serials, appliance/routing IDs, and preshared keys before saving evidence.
3. Pair through the Homey credential view. Never paste credentials into a terminal, script, environment variable, document, screenshot, or chat.
4. Compare every monitoring capability with Watersystems/API observations.
5. Record the current automatic-flushing state. Exercise device control and both Flow actions one at a time, waiting for confirmation after each. Do not retry an ambiguous command automatically.
6. Restore the recorded original state and verify it in both Homey and GROHE.
7. Exercise failure handling using a mock or controlled app-level network failure when disconnecting the real hub could affect other automations.
8. Sanitize evidence, repeat local checks and secret review, then change only directly evidenced PENDING rows to PASS.

## Live evidence log

PENDING. The controller/owner must complete this section during the separately authorized external phase. Do not infer or fabricate results.
